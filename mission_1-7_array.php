//<?php
//$filename = 'mission_1-7.txt';
//echo $filename;

//$lines = file($filename);  // ファイルを配列に格納し、さらに変数に格納

//?>

//<ul>

//<!-- foreachでファイルの配列をループ処理 -->
//<?php foreach ($lines as $line) : ?>

  //<!-- 配列の要素を1行ずつ<li>タグに埋め込む -->
  //<li><?php echo $line; ?></li>

//<?php endforeach; ?>
//</ul>

//$fp = fopen($filename, 'r');

//while (! feof ($fp)) {
   //$load = fgets ($fp, 4096); 
   //echo $load; //txtを1行ずつ読み込んで表示
//}

//fclose ($fp) 

//?>

//$array = array('fruits'=>array('a'=>'orange', 'b'=>'banana'), 'numbers'=>array(1, 3, 5));
